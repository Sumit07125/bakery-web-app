  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Swad Dhulekar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="mx-auto my-2 my-lg-0 w-100 d-flex justify-content-center">
          <form class="d-flex search-form w-100 px-lg-3" action="/search" method="GET" style="max-width: 600px;">
            <div class="input-group shadow-sm rounded-pill overflow-hidden bg-white">
              <input class="form-control border-0 shadow-none ps-4 bg-transparent" type="search" name="query" placeholder="Search for treats..." aria-label="Search">
              <button class="btn btn-primary px-4 border-0 m-0 rounded-0" type="submit"><i class="bi bi-search"></i></button>
            </div>
          </form>
        </div>
        <div class="d-flex align-items-center mt-2 mt-lg-0 gap-3">
      <% if (locals.user) { %>
        <a href="/my-bills" class="btn btn-outline-primary d-flex align-items-center justify-content-center gap-2 shadow-sm px-3 text-nowrap">
          <i class="bi bi-receipt"></i> My Orders
        </a>
        <a href="/cart" class="btn btn-outline-primary d-flex align-items-center justify-content-center gap-2 shadow-sm px-3 text-nowrap">
          <i class="bi bi-cart"></i> Cart
          <% if (locals.cartCount > 0) { %>
            <span class="badge rounded-pill bg-danger"><%= locals.cartCount %></span>
          <% } %>
        </a>
        <div class="dropdown">
          <button class="btn btn-warning dropdown-toggle d-flex align-items-center justify-content-center gap-2 shadow-sm px-4" type="button" id="profileMenu" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-circle fs-5"></i> My Account
          </button>
          <ul class="dropdown-menu dropdown-menu-end border-0 shadow-lg rounded-3 mt-2" aria-labelledby="profileMenu">
            <li><a class="dropdown-item py-2" href="/profile"><i class="bi bi-person me-2 text-secondary"></i>Profile</a></li>
            <li><a class="dropdown-item py-2" href="/cart"><i class="bi bi-cart me-2 text-secondary"></i>Cart</a></li>
            <li><a class="dropdown-item py-2" href="/my-bills"><i class="bi bi-receipt me-2 text-secondary"></i>My Orders</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item py-2 text-danger" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
          </ul>
        </div>
      <% } else { %>
        <div class="d-flex flex-row flex-nowrap gap-2">
          <a href="/user_login" class="btn btn-outline-primary d-flex align-items-center justify-content-center gap-2 shadow-sm px-3"><i class="bi bi-box-arrow-in-right"></i> Login</a>
          <a href="/register" class="btn btn-primary d-flex align-items-center justify-content-center gap-2 shadow-sm px-3"><i class="bi bi-person-plus"></i> Register</a>
          <a href="/admin_login" class="btn btn-outline-secondary d-flex align-items-center justify-content-center gap-2 shadow-sm px-3"><i class="bi bi-shield-lock"></i> Admin</a>
        </div>
      <% } %>
        </div>
      </div>
    </div>
  </nav>
